#include <config.h>

void DrawUpdateGraph(HWND, struct world, int, int);
void DrawGraph(HWND, int, int);

#define GRAPH_SIZE_RATIO 0.6
#define GRAPH_TICK_TOTAL 16

#define GRAPH_ORIGIN_OFFSET 0.1  // % of graph area
